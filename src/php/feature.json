{
    "id": "php",
    "name": "PHP",
    "options": {
        "version": {
            "type": "string",
            "proposals": ["latest", "8.0.16"],
            "default": "latest",
            "description": "Select or enter a PHP version"
        },
        "installComposer": {
            "type": "boolean",
            "default": true,
            "description": "Install PHP Composer?"
        },
        "overrideDefaultVersion": {
          "type": "boolean",
          "default": "true",
          "description": "Sets or overrides the default PHP version"
        }
    },
    "extensions": [
        "xdebug.php-debug",
        "bmewburn.vscode-intelephense-client",
        "xdebug.php-pack",
        "devsense.phptools-vscode"
    ],
    "containerEnv": {
        "PHP_PATH": "/usr/local/php/current",
        "PATH":"${PHP_PATH}:${PHP_PATH}/bin:${PATH}"
    },
    "install": {
        "app": "",
        "file": "install.sh"
    }
}