{
    "id": "git-lfs",
    "name": "Git Large File Support (LFS)",
    "options": {
        "version": {
            "type": "string",
            "enum": ["latest", "none"],
            "default": "latest",
            "description": "Currently unused."
        }
    },
    "install": {
        "app": "",
        "file": "install.sh"
    }
}