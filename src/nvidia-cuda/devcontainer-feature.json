{
  "id": "nvidia-cuda",
  "name": "Nvidia CUDA",
  "description": "Installs Nvidia's CUDA libraries.",
  "version": "1.0.0",
  "options": {
    "install_cuda": {
      "type": "boolean",
      "default": true,
      "description": "Install CUDA libraries"
    },
    "install_cudnn": {
      "type": "boolean",
      "default": true,
      "description": "Install cuDNN libraries"
    }
  }
}
