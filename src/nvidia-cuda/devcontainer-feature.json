{
  "id": "nvidia-cuda",
  "name": "NVIDIA CUDA",
  "description": "Installs NVIDIA CUDA libraries.",
  "version": "1.0.0",
  "options": {
    "install_cuda": {
      "type": "boolean",
      "default": true,
      "description": "Install essential CUDA libraries"
    },
    "install_cudnn": {
      "type": "boolean",
      "default": true,
      "description": "Install CUDA Deep Neural Network (cuDNN) library"
    }
  }
}
